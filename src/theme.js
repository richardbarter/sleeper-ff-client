import { ThemeProvider, createTheme } from "@mui/material";


const Theme = createTheme({

  palette: {
    primary: {
      main: '#00cc66'
    },
    secondary: {
      main: '#FFFFFF'
    }
  }
})

export default Theme